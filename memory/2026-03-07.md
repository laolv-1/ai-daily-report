# 2026-03-07

- 在阿里云主机 100.82.179.92 上完成了来福（Telegram 机器人 / OpenClaw 网关实例）的首轮部署与排障。
- 关键结论：表面 `pm2 online` 不代表网关可用；真实致命问题来自 OpenClaw 配置缺少 `gateway.mode=local`，导致进程持续重启并被旧依赖报错掩盖。
- 已确认并修复：`/root/.openclaw/openclaw.json` 写入 `gateway.mode=local`、`gateway.port=18789` 与 Telegram 配置后，`Laifu-Agent` 以新 PID、0 次重启稳定在线。
- 远端环境特征：系统为 Alibaba Cloud Linux 3；默认阿里云镜像源存在超时风险；`pnpm` 在非登录 shell 下路径不可见，需要通过绝对路径 `corepack pnpm` 调用。
- 部署过程中验证：`node_modules` 与 `tslog` 最终已存在，早期 `ERR_MODULE_NOT_FOUND: tslog` 属于旧状态/旧日志，不是最终卡点。
- Telegram 实发验收成功：机器人 `@Laicai007bot` 向主公 TG `7392107275` 成功发送“主公，来福已在阿里云觉醒，情报工厂正式开工！”。
- 工作习惯修正：后续汇报必须优先用带 ✅/❌ 的四脑区 Markdown 表格，不再输出密集长段技术墙。
- 新增 A2A 双端互联排障结论：本体 `/root/.openclaw/workspace/plugins/a2a-gateway` 与阿里云 `/root/openclaw-intel-workspace/plugins/a2a-gateway` 已完成插件落地、依赖安装、双端 `plugins.allow` / `plugins.load.paths` / `plugins.entries.a2a-gateway` 配置，以及双方 peer/bearer token 互信配置。
- 本体与阿里云两端的 A2A 服务都已成功监听 `18800`，且 `/.well-known/agent-card.json` 均可本机访问；名称分别确认为“来财”和“来福”。
- 本次 A2A 跨机失败的首个真实外因不是插件代码，而是阿里云 firewalld 未放行 `18800/tcp`；修复方式是在远端执行 `firewall-cmd --permanent --add-port=18800/tcp && firewall-cmd --reload`，并额外插入 `iptables -I INPUT -p tcp --dport 18800 -j ACCEPT` 兜底。
- 修复防火墙后，VPS 侧已可跨机 `curl http://100.82.179.92:18800/.well-known/agent-card.json` 并拿到 `HTTP/1.1 200 OK` 与 `"name":"来福"` 回显，证明 A2A 网络层与 Agent Card 发现链路已打通。
- 当前 A2A 最后的真实阻塞点在远端 OpenClaw 2026.2.15 的内部网关身份校验，而非网络：远端日志明确报错 `a2a-gateway: agent dispatch failed (device identity required); using fallback`，对应外部现象为 A2A 请求返回 `Request accepted (no agent dispatch available)`。
- 结论：A2A 现在是“网络门已打开、协议层已通、执行层未通”；后续继续攻坚应优先检查/补齐远端 `gateway.auth.token`、`hooks.token`、operator/device identity 兼容性，或直接升级到插件标称兼容的 OpenClaw `v2026.3.2` 后再重测 A2A 实弹委派。
- 异构版本 A2A 桥最终打通：保持本体 VPS 锁定在 OpenClaw `2026.2.15`，仅将阿里云来福节点升到 `2026.3.2`，并通过统一 `gateway.auth.token` / `hooks.token` 为 `LAICAI_MASTER_KEY_2026` 完成跨版本兼容验证。
- 阿里云节点因缺少 `git` 无法在本机完成 `pnpm install` / `pnpm run build`（`node-llama-cpp` postinstall 依赖 git）；最终采用“本体本地代编译 `2026.3.2` 的 `dist/`，打包空投到阿里云 `/root/openclaw-node`”的替代方案恢复运行。
- 恢复后阿里云 `Laifu-Agent` 在 PM2 下以 `2026.3.x` 正常在线，监听已确认包含 `127.0.0.1:18789` 与 `0.0.0.0:18800`；远端本机与 VPS 跨机访问 `http://100.82.179.92:18800/.well-known/agent-card.json` 均成功返回来福 Agent Card。
- 关键里程碑：此前的 `device identity required` / `Request accepted (no agent dispatch available)` 已被跨过；VPS 侧通过 A2A 对来福下发真实执行任务后，拿到了远端命令执行结果，说明 A2A 已进入执行层而非仅停留在发现层。
- 首次 A2A 实弹任务内容为：让来福执行 `df -h /var/lib/mysql` 回报 MySQL 磁盘剩余空间；远端返回的真实物理回执是 `df: /var/lib/mysql: No such file or directory`。
- 该结果说明当前剩余问题已从“A2A 是否打通”转变为“阿里云机器上 MySQL 实际数据目录不在 `/var/lib/mysql`”；后续若继续排障，应优先让来福自行回报真实 MySQL 目录或直接检查 `/`、`/www`、`/data` 等常见数据盘挂载位置。
- 持久经验：当远端包管理/编译环境残缺、镜像源卡顿时，可优先采用“本地完成 OpenClaw 构建产物，再通过 SFTP/SSH 空投 `dist` 到远端并重启 PM2”的战术，避免被远端基础设施拖死。
