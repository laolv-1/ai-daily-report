- 2026-03-08：MolTBook 审批管线完成 SSS 级紧急纠偏。VPS 时区已切到 Asia/Shanghai，并在 crontab 中写入 `CRON_TZ=Asia/Shanghai`；审批与双擎日报任务均锁定为北京时间 `0 0,12 * * *`。MolTBook 审批脚本 `/root/.openclaw/workspace/scripts/cyber_exchange_audit.py` 已重构为：通过 paramiko 跨网连接阿里云 `100.82.179.92` 读取 `/www/wwwroot/spider_center/molt_learning/` 下 `.draft`，不再本地幻想路径；无新件或低价值件时 `SystemExit(0)` 绝对静默，不向 Telegram 发送“未发现”等噪音。对应 cron 包装脚本为 `/root/.openclaw/workspace/scripts/cyber_exchange_audit.cron.sh`。workspace 提交：`8d54ec3` (`Fix MolTBook approval pickup timing and silent mode`)。
- 2026-03-08：主公采纳了阿里云草稿 `20260308-000025-broad-scan.draft` 中关于“Agent 在单次会话第 8 次工具调用后准确率明显下降”的情报，并要求固化为系统护城河。已新增技能 `/root/openclaw/skills/skill_context_refresh.md`，明确“8 次 Tool Call 警戒线、重任务前落盘断点、清上下文、换新会话再战、蜂群统一执行拉尔夫循环（cron 拉起 -> 单轮执行 -> 入库/落盘 -> 退出）”。同时新增本体防降智脚本 `/root/.openclaw/workspace/scripts/context_refresh_guard.py` 与 cron 包装 `/root/.openclaw/workspace/scripts/context_refresh_guard.cron.sh`，每 6 小时执行一次，清理旧 session、旧报表与过大日志；实跑时已删除多条 `/root/.openclaw/agents/main/sessions/*.jsonl`，当前只保留少量较新 session。workspace 提交：`3c02aa1` (`Add context refresh guardrail`)。
- 2026-03-08：主公采纳了“五核工具优先调度”逻辑，已固化技能 `/root/openclaw/skills/skill_tool_roi_scheduler.md`。技能核心是高压任务优先使用高 ROI 的五类工具：读、改、跑、跨网、发；低 ROI 工具延后，且工具调用逼近 8 次时主动收束、落盘、换新上下文。
- 2026-03-08：为彻底封死 MolTBook 审批时间失控，除了 `CRON_TZ=Asia/Shanghai + 0 0,12 * * *` 外，还在 `/root/.openclaw/workspace/scripts/cyber_exchange_audit.py` 中新增 `enforce_bjt_delivery_window()` 硬闸门：只有北京时间 `00:00` 与 `12:00` 才允许 Telegram 审批发送，其他任何时间即便手动误触脚本也会静默 `SystemExit(0)` 退出。现场测试回执为 `WINDOW_LOCKED_NOW 0`，证明非窗口时段已被硬锁死。workspace 提交：`641affe` (`Lock approval delivery to BJT windows and add update log`)。
- 2026-03-08：主公要求建立“系统变更日志”铁律。已生成今日首份更新日志 `/root/.openclaw/workspace/reports/2026-03-08_update_log.md`，内容涵盖：`skill_tool_roi_scheduler.md`、`skill_context_refresh.md`、MolTBook 时间死锁逻辑、跨网取件与静默机制、当前 cron 基线。并已通过 paramiko 静默投递到 Win10（`100.89.160.67`，SSH 用户 `Administrator`）路径：`D:/来财/系统更新日志/2026-03-08_update_log.md`，远端回执：`REMOTE_OK ... SIZE 1546`。这说明 Win10 SSH 确认可用，后续可继续按该方式归档系统变更。
- 2026-03-08：来福（阿里云 `100.82.179.92`）的国内情报链路再次复核，仍符合“用完即毁”的拉尔夫循环：crontab 保持 `*/30 * * * * /usr/bin/python3 /opt/intel_spider/spider_v3.py` 与 `0 9,21 * * * /usr/bin/python3 /opt/intel_spider/push_v3.py`；现场未发现常驻 `spider_v3.py` / `push_v3.py` Python 进程，说明当前仍是 cron 拉起 -> 执行 -> 入库 -> 退出，而非长期常驻记忆进程。
- 2026-03-08：已新增 SSS 级“全域 AI 变现市场调研与 A2A 扩军雷达”能力。新脚本为 `/root/.openclaw/workspace/scripts/market_research_daily.py`，cron 包装为 `/root/.openclaw/workspace/scripts/market_research_daily.cron.sh`。功能：每天生成仅含 Markdown 表格的《每日AI变现与A2A扩军调研报告》，字段固定为“赛道/平台｜成本档位｜AI 变现玩法｜需增加的A2A设备｜预期收益与风控”，并强制覆盖 Tier 0 / Tier 1 / Tier 2 三档成本分级。
- 2026-03-08：`market_research_daily.py` 的数据源已定型为“双擎混编”：一是通过 paramiko 跨网连接阿里云 `100.82.179.92`，读取 `intel_station.intel_articles` 最近 72 小时内部情报；二是直接抓取 Reddit JSON（`r/Entrepreneur`、`r/SaaS`、`r/smallbusiness`、`r/Fiverr`、`r/Upwork`）做外部市场热度补强。Brave `web_search` 当场因未配置 API key 无法使用，已改走稳定可抓取源，不阻塞任务推进。
- 2026-03-08：市场调研分发链路已打通并符合主公硬要求：Telegram 端固定走来福 Bot `8545151429:AAGTiHUsUsH_VkYEtswD3I2v_7pDV9DO8S0`，Win10 端固定通过 paramiko 静默桥接到 `D:/来财/市场调研/[日期]_ClawWork.md`；VPS 本地禁止保留报告正文，发送与归档后立即清理，仅保留脚本、日志和状态文件。为杜绝运行碎屑，本次已补上 `cleanup_local_artifacts()`，自动删除 `/root/.openclaw/workspace/scripts/__pycache__`。
- 2026-03-08：市场调研 cron 已加入本体 crontab，基于既有 `CRON_TZ=Asia/Shanghai` 在北京时间每日 `12:30` 执行：`30 12 * * * /root/.openclaw/workspace/scripts/market_research_daily.cron.sh`。这与当前双擎日报、MolTBook 审批、心跳监控、灵魂备份、系统变更日志 watchdog 一起，构成新的日常自动化控制面。
- 2026-03-08：`market_research_daily.py` 已现场手动试跑成功。最新实物回执：`{"last_run_bjt": "2026-03-08 17:57:48", "telegram_message_id": 44, "win10_remote_file": "D:/来财/市场调研/2026-03-08_ClawWork.md", "win10_size": 1121, "internal_count": 10, "reddit_count": 12}`。这证明来福 Telegram 首份调研表已发出、Win10 已落盘、内部信号 10 条与 Reddit 外探 12 条均已进入报告。
- 2026-03-08：Win10 侧已再次验证市场调研归档存在，PowerShell 回执显示 `D:/来财/市场调研/2026-03-08_ClawWork.md` 文件存在且写入时间正常；workspace 已完成提交 `5055076`，提交信息：`Add daily market research radar`。
